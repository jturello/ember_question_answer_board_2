import { manyAnswersIcon } from 'question-answer-board/helpers/many-answers-icon';
import { module, test } from 'qunit';

module('Unit | Helper | is favorite');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = manyAnswersIcon([42]);
  assert.ok(result);
});
